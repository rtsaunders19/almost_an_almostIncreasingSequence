Given a rectangular matrix of characters, add a border of asterisks(*) to it.

picture = ["abc",
           "ded"]

addBorder(picture) = ["*****",
                      "*abc*",
                      "*ded*",
                      "*****"]
